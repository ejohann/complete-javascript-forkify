// Global app controller
import str from './models/Search';

import {add as a, multiply as m} from './views/searchView';

console.log(`Using imported functions add: ${a(9, 6)} and multiply: ${m(9, 6)}`);